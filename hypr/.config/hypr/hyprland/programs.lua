-- Shared programs (wiki: Configuring > Core > Autostart / Dispatchers).
-- Single source of truth for commands reused by autostart and binds.

local M = {
    terminal = "foot",
    menu = "wofi --show drun",
}

return M
